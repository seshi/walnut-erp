[build]
builder = "NIXPACKS"
buildCommand = "prisma generate && npm run build"

[deploy]
startCommand = "prisma db push --accept-data-loss && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
